% =====================================================================
\section{Experiments}
\label{sec:experiments}
% =====================================================================

% TODO: ~3 LNCS pages. NEW section. Drafted after experiments complete.
%
% E1 baseline: lambda=0, static environment. Sanity check (one paragraph).
%
% E2 lag under shock: discrete s* shift; sweep lambda. Headline figure:
%     adaptation timescale vs. lambda. Show that high-lambda communities
%     persist in the old regime well past the environmental change.
%
% E3 hysteresis under slow drift: slow s* random walk; sweep lambda up
%     then down. Headline figure: hysteresis loops in (g, order parameter)
%     space, area scaling with lambda. THIS IS THE PAPER'S KEY RESULT.
%
% E4 dissociation: same environment sequence run on (a) our model,
%     (b) minimal Bayesian rule-induction baseline. Headline figure:
%     side-by-side trajectories showing symmetric tracking vs hysteretic.
%
% Each: >=20 random seeds, mean +/- 95% CI bands.
